rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite lectura y escritura para sincronización en tiempo real del estacionamiento
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
